# Clean Temp Folder Script

This Python script cleans the Windows temporary folders: `%temp%`, `C:\Windows\Temp`, and `Prefetch`.

## Requirements

- Python 3.x
- Modules used: `os` (built-in)

## How to use

1. Run the script with Python:

```bash
python clean_temp.py
